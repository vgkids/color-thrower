<%= form_for(@throw) do |f| %>
  <% if @throw.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@throw.errors.count, "error") %> prohibited this throw from being saved:</h2>

      <ul>
      <% @throw.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
